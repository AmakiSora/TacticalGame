# Deploy credentials - copy to .env.deploy and fill in
# WARNING: Do not commit .env.deploy to git!

DEPLOY_HOST=your-server-ip
DEPLOY_PORT=22
DEPLOY_USER=root
DEPLOY_PASSWORD=your-password-here
DEPLOY_REMOTE_BASE=/srv/tactical-game

# Optional: Pre-set control token for the deployed server
# If not set, a random token will be generated on each deploy
# CONTROL_TOKEN=your-preferred-control-token