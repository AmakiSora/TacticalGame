# RL 模型说明

本文按“一个模型一个大标题”组织。以后新增模型时，应新增一个独立的二级标题，并完整填写该模型的文件、版本、网络、输入输出、训练数据、对手、策略、性能、部署方式和兼容性；不要只在总表里追加一行。

资料来源：`rl/RELEASE_NOTES.md`、`rl/README.md`、`rl/env.py`、`rl/env_v200.py`、`rl/local_env.py`、`rl/train.py`、`rl/evaluate_cross.py`、模型 zip 内的 `data`/`system_info.txt`，以及 `rl/tb/` TensorBoard 日志。性能数字只对注明的评估协议负责，不代表所有地图、座位或真人对手上的泛化能力。

## 模型状态总表

| 模型文件 | 环境版本 | 状态 | 说明 |
|---|---|---|---|
| `hex_ppo_v2.2.0_20260827_default_modelmix_best.zip` | v2.2.0 | **当前推荐** | 对 v2.0.0 真身 16:0，对 v2.1.3 累计 16:2；交付文件即评估 best 断点 |
| `hex_ppo_v2.0.0_20260824_default_rule_500000.zip` | v2.0.0 | 基准/历史 | 唯一能在主场满血出战的旧模型；v2.1.x 的模型对手与验收基准都是它 |
| `hex_ppo_v2.1.3_20260826_default_rule_defensive_800000.zip` | v2.1.3 | 历史 | 单一 defensive 对手训练；弱于 v2.0.0、强于 v2.1.4–v2.1.8 |
| `hex_ppo_v2.1.1_20260825_default_rule_mixed_500000.zip` | v2.1.1 | 历史 | 早期 v2.1 模型，跨版本评估中弱于 v2.0.0 |
| `hex_ppo_v2.1.8_20260827_default_modelmix_920000.zip` | v2.1.8 | **作废** | 在回合交替损坏的环境下训练（见模型 9），自评 100% 胜率为虚假数据 |
| `hex_ppo_v2.1.6_20260827_default_modelmix_800000.zip` | v2.1.6 | **作废** | 训练环境仍丢失占点奖励信号，互打全败 |
| `hex_ppo_v2.1.5_20260827_default_modelmix_600000.zip` | v2.1.5 | **作废** | 移动只走一格 + 无占点信号环境下训练，互打全败 |
| `hex_ppo_v2.1.4_20260826_default_modelmix_800000.zip` | v2.1.4 | **作废** | 模型对手动作映射错位，训练对手实为噪声 |
| `hex_ppo_v1.0.0_20260824_default_random_opponent_120000.zip` | v1.0.0 | 历史 | 512 动作，仅由 `run_model_v100.py` 路由 |
| `hex_ppo_v1.0.0_20260824_default_random_opponent_100096.zip` | v1.0.0 | 历史 | 同上，更早的实验模型 |

注意：不存在 v2.1.7 模型文件（该版本训练在约 13 万步处因对手回合规避崩溃而中断，其续训产物即上表的 v2.1.8 文件，同样作废）。

## 模型 1：hex_ppo_v2.2.0_20260827_default_modelmix_best

### 身份与文件

- **文件**：`rl/models/hex_ppo_v2.2.0_20260827_default_modelmix_best.zip`（来源：`rl/checkpoints/default/20260827-182746/best/best_model.zip`）
- **定位**：**当前默认推荐模型**，首个在公平座位下打赢 v2.0.0 真身的模型。
- **训练环境版本**：v2.2.0（2026-08-27），含 v2.1.9 的回合交替修复。
- **地图/对手**：`default` 地图，`modelmix` 对手：50% 概率使用 v2.0.0 模型对手，其余在 aggressive / mixed 规则风格间切换；每局随机分配智能体座位（player_a / player_b 各 50%）。
- **训练步数**：800,000；交付的是训练中评估选出的 best 断点，非终点快照（终点快照对 v2.0.0 仅 0:8，后期震荡退化）。
- **推荐运行器**：`rl/run_model.py`；服务端按动作空间 54 自动路由。

### 网络、观察和动作

网络结构与其余 54 动作模型相同：两个 `3922 → 64 → 64` 的 Tanh MLP（actor/critic 分离），action head 54。观察为 3,922 维（217 格 × 18 特征 + 16 全局特征），相对视角编码。

动作空间 `Discrete(54)`：`end_turn` 1 个、12 个稳定单位槽 × 4 意图（`move`/`attack`/`heal`/`special`）、5 个部署槽。移动候选为离战略目标最近的可达格（BFS 绕路，一步直达，与引擎“一次移动可达任意格”的语义对齐）。

### 训练过程与关键环境语义

- 超参数沿用标准配置（`learning_rate=3e-4`、`n_steps=256`、`batch_size=64`、`n_epochs=10`、`gamma=0.99`、`ent_coef=0.01`、`clip_range=0.2`），CUDA 训练，评估每 10,000 步 × 20 局。
- **奖励在对手响应之后结算**（v2.1.7 起恢复）：占点在回合边界结算，若像 v2.1.0–v2.1.6 那样立即结算，占点收益会被基线更新吸收，模型将不再抢点。
- **座位随机化**（v2.2.0）：智能体坐 player_b 时，v2.0.0 模型对手恰好坐在它的主场 player_a，相对视角编码与其原生编码完全一致，对手即满血真身；坐 player_a 时对手为退化形态。
- 依赖两个更底层的修复，否则训练数据全部失真：
  - 动作后必须刷新状态再进对手回合（引擎只在 `end_turn` 校验回合，陈旧状态会让对手回合被跳过，智能体每回合白嫖 1 个先手动作）；
  - 移动动作必须一步走到最优可达格（引擎允许一步移动任意可达格）。

### 性能记录

评估协议：`rl/evaluate_cross.py`，确定性策略，`default` 地图，15 回合裁决分定胜负。

| 对局 | 结果 |
|---|---|
| vs `v2.0.0`（v2.0.0 坐主场 player_a） | **16:0**（两轮合计 23:1） |
| vs `v2.1.3`（两种座位） | **8:0** / 6:2 |

训练期评估（混合对手、含约半数满血 v2.0.0）：胜率从 35% 起步，中期峰值约 90%，终点回落到 45%——后期震荡正是交付 best 而非终点模型的原因。自 v2.2.1 起训练收尾自动交付 best 断点。

### 部署与兼容性

- 双座位可战（训练覆盖两个座位），`run_model.py` 任意座位均可。
- 仅适用于普通双人顺序地图；`standoff`/`artillery-zone` 等需要单独环境。
- 可在当前环境续训（`RL_LOAD_MODEL` 指向本文件），但继续训练可能重现后期震荡，建议续训时缩短步数或调低学习率。

## 模型 2：hex_ppo_v2.0.0_20260824_default_rule_500000

### 身份与文件

- **文件**：`rl/models/hex_ppo_v2.0.0_20260824_default_rule_500000.zip`
- **定位**：**历史基准模型**。v2.1.x 系列的训练对手与验收标尺；保留用于旧版本大厅和跨版本离线对战。
- **训练环境版本**：v2.0.0（2026-08-24）。首个规则对手 + 固定意图槽位版本。
- **地图/对手**：`default` 地图，单一 `rule` 对手。
- **训练步数**：目标 500,000；`num_timesteps=500,224`。参数量 512,999；约 6.26 MB。
- **推荐运行器**：`rl/run_model_v200.py`；必须搭配不可变环境快照 `rl/env_v200.py`。

### 网络、观察和动作

两个 `3922 → 64 → 64` 的 Tanh MLP；action head 38。观察 3,922 维，但当时编码**固定以 `player_a` 为己方视角**。

动作空间 `Discrete(38)`：`end_turn` 1 个、8 个单位槽 × 4 意图 32 个、5 个部署槽。单位每步按（兵种, q, r, -hp）排序取前 8 分槽（槽位跨状态不稳定）；移动要求严格接近目标（不绕路）。这两点语义差异是“旧模型对手”必须用 `legacy` 动作表复现的原因。

### 训练过程与对手

MaskablePPO 标准超参数；本地 TypeScript worker；规则对手优先级：攻击 → 治疗 → 补给 ≥90 部署 → 移动。奖励在对手响应后结算（该时机后被证明是正确的，v2.1.7 恢复）。

### 性能记录

无周期评估标量，仅 rollout 日志（平均回报 -7.60 → 20.76）。跨版本评估（`evaluate_cross.py`，确定性，15 回合裁决）：对 v2.1.1–v2.1.8 各版本保持压制（见各模型条目）；镜像对局证明 `default` 地图无先手优势。重要限制：**只能坐在 player_a 满血**——坐 player_b 时固定视角编码严重分布外（实测训练形态被真身 8:0）。

### 部署与兼容性

必须使用 `run_model_v200.py` + `env_v200.py`，尽量固定 player_a。38 动作不能加载进 54 动作环境；作为训练对手时由 `env.py` 的 `legacy` 分支驱动（38 动作语义 + 相对视角观测）。

## 模型 3：hex_ppo_v2.1.3_20260826_default_rule_defensive_800000

### 身份与文件

- **文件**：`rl/models/hex_ppo_v2.1.3_20260826_default_rule_defensive_800000.zip`
- **定位**：历史模型；v2.1.4–v2.1.8 时期的对比标尺。
- **训练环境版本**：v2.1.3（2026-08-26，仅文件命名规范变更）。
- **地图/对手**：`default` 地图，**单一 `defensive` 规则对手**（文件名 `rule_defensive`；注意不是 mixed），800,000 步。
- **推荐运行器**：`rl/run_model.py`（54 动作）。

### 已知问题与性能

该模型训练于移动只走一格、奖励无占点信号的 v2.1 环境（见模型 8），棋力上限有限，但在互打中稳定强于 v2.1.4–v2.1.8（对比 8:0 / 14:0），弱于 v2.0.0（两种座位均 0:4 以上）。它对单一对手脚本过拟合的风险较高，与 modelmix 系列的横向对比需注意训练条件不同。

## 模型 4：hex_ppo_v2.1.1_20260825_default_rule_mixed_500000

### 身份与文件

- **文件**：`rl/models/hex_ppo_v2.1.1_20260825_default_rule_mixed_500000.zip`
- **定位**：早期 v2.1 历史模型，非部署模型。
- **训练环境版本**：v2.1.1（2026-08-25），mixed 规则对手，500,000 步。参数量 514,039。
- **推荐运行器**：`rl/run_model.py`。

### 性能记录

训练期评估（20 局/次，mixed 规则对手）：平均胜率全程均值 38.5%、单点最高 70%；平均据点 2.09。跨版本抽样中固定坐 player_b 时对 v2.0.0 0:4。与后续版本一样受“移动一格 + 无占点信号”环境缺陷影响，不应以当时自评数字推断真实棋力。

## 模型 5：hex_ppo_v1.0.0_20260824_default_random_opponent_120000

### 身份与文件

- **文件**：`rl/models/hex_ppo_v1.0.0_20260824_default_random_opponent_120000.zip`
- **定位**：v1 训练流程的默认图历史模型；不是当前部署模型。
- **训练步数**：`num_timesteps=120,000`；约 6.63 MB；参数量 543,809。
- **环境特征**：观察 3,922 维，动作 `Discrete(512)`；动作索引表示旧环境合法动作列表中的第 N 项。
- **训练设备记录**：PyTorch 2.13.0 CPU（zip 生成时的历史信息）。

### 网络与动作

两个 `3922 → 64 → 64` Tanh MLP，action head `512 × 64 + 512`。512 个动作不是稳定意图槽；每个状态重新生成合法动作列表，由 mask 屏蔽不可用索引，不能解释为固定语义。

### 兼容性

`Discrete(512)` 与 38/54 完全不兼容；服务端路由到 `run_model_v100.py` + `env_v100.py` 快照。随机对手训练，无统一胜率评估。

## 模型 6：hex_ppo_v1.0.0_20260824_default_random_opponent_100096

### 身份与文件

- **文件**：`rl/models/hex_ppo_v1.0.0_20260824_default_random_opponent_100096.zip`
- **定位**：更早的 v1 随机对手实验模型；`num_timesteps=100,096`；参数量 543,809。
- **观察/动作**：同模型 5（3,922 维 / `Discrete(512)`）。
- 仓库没有统一胜率评估，不能按步数推断与模型 5 的强弱；适合作旧环境回归样本。由 `run_model_v100.py` 路由。

## 模型 7：v2.1.4 / v2.1.5 / v2.1.6（作废批次）

三个模型文件：

- `hex_ppo_v2.1.4_20260826_default_modelmix_800000.zip`
- `hex_ppo_v2.1.5_20260827_default_modelmix_600000.zip`
- `hex_ppo_v2.1.6_20260827_default_modelmix_800000.zip`

**状态：全部作废，保留仅作历史对照。** 它们分别训练于三个被后续证实有缺陷的环境：

| 版本 | 环境缺陷（事后确认） |
|---|---|
| v2.1.4 | 模型对手 38 动作按下标硬映射到 54 动作表，槽位与移动语义错位，对手实为噪声 |
| v2.1.5 | 移动动作只走一格（引擎本可一步直达），机动效率 1/3–1/5，连带抑制部署 |
| v2.1.6 | 移动已修，但奖励立即结算吞掉了占点收益（权重最大项 90/点），策略不抢点 |

互打成绩（`evaluate_cross.py`，8 局/组）：三者对 v2.0.0、对 v2.1.3 均 0:8，换座位同样全败。这些模型的自评胜率不能作为棋力依据。

## 模型 8：hex_ppo_v2.1.8_20260827_default_modelmix_920000（作废）

### 身份与文件

- **文件**：`rl/models/hex_ppo_v2.1.8_20260827_default_modelmix_920000.zip`（v2.1.7 训练中断后从 12 万步断点续训的产物；不存在独立的 v2.1.7 模型文件）
- **状态**：**作废**。

### 作废原因：回合交替损坏

v2.1.7 改动误删了“智能体动作后、对手阶段前”的状态刷新：对手回合循环读到陈旧状态被跳过；而引擎只在 `end_turn` 时校验回合归属，智能体的单位动作在对手回合照样执行——**每回合白嫖 1 个先手动作并偷走对手 1 点 AP**。该环境下训练自评 100% 胜率，换到正确交替的互打环境立刻 0:8（对 v2.0.0）/ 0:14（对 v2.1.3，两种座位）。

教训已写入 `rl/RELEASE_NOTES.md` v2.1.9 条目：训练自评胜率异常时必须先做交叉验证。

## 模型检查点与测试快照

这些文件是训练时间切片或接口测试文件，不是独立策略；观察、动作和兼容性与其所属模型一致。

- `rl/checkpoints/default/20260824-034840/`：v2.0.0，20,000–500,000 步，`best/` 为 500,000 步版本。
- `rl/checkpoints/default/20260825-053124/`：v2.1.1，20,000–500,000 步，`best/` 对应 480,000 步。
- `rl/checkpoints/default/20260826-050439/`：v2.1.3（defensive 对手），至 800,000 步。
- `rl/checkpoints/default/20260826-151230/`：v2.1.4，至 800,000 步。
- `rl/checkpoints/default/20260827-010723/`：v2.1.5，至 600,000 步。
- `rl/checkpoints/default/20260827-044352/`：v2.1.6，至 800,000 步。
- `rl/checkpoints/default/20260827-124430/`：v2.1.7→v2.1.8 续训运行（首次运行在约 13 万步处崩溃），至 920,000 步。
- `rl/checkpoints/default/20260827-182746/`：**v2.2.0，至 800,000 步；`best/best_model.zip` 即当前推荐模型的来源**。
- `rl/checkpoints/default/`：v1 随机对手的 20,000–120,000 步 checkpoint 和 best 文件。
- `rl/test-output/`：仅存历史评估脚本（`eval_candidates.py` / `eval_final.py`），接口测试模型快照已清理。

检查点文件名中的 `_steps` 是保存时累计步数；正式模型文件名最后的数字是训练目标步数（续训产物的实际步数可能更高，以 zip 内 `num_timesteps` 为准）。自 v2.2.1 起，`rl/models/` 的交付文件为评估 best 断点而非训练终点。

## 所有模型共享的版本演进与路由规则

| 版本 | 关键变化 | 兼容方式 |
|---|---|---|
| v1.0.0 | Gymnasium + MaskablePPO；随机合法动作；`Discrete(512)` | 只能使用 v1 快照环境 |
| v2.0.0 | 规则对手；固定意图槽；奖励塑形；`Discrete(38)` | `env_v200.py` + `run_model_v200.py` |
| v2.0.1–v2.0.4 | CUDA 选择、断点/检查点、时间戳命名和 zip 后缀修复 | 不改变 v2.0 契约 |
| v2.1.0 | 即时奖励归因、BFS 移动、12 稳定槽、mixed 对手、`Discrete(54)`、相对视角 | 必须从零训练（该版本埋下移动一格与奖励吞占点两个缺陷） |
| v2.1.1 | 修复空槽治疗崩溃 | v2.1.0 模型可继续使用 |
| v2.1.2 | 保存 v1/v2.0 快照/运行器，按动作空间自动路由 | 512/38/54 分别使用各自运行器 |
| v2.1.3 | 统一模型文件命名，版本号和日期前置 | 新模型使用版本前置格式 |
| v2.1.4 | 引入 v2.0.0 模型对手（映射错误，已废弃实现） | 模型作废 |
| v2.1.5 | 模型对手改用 `legacy` 动作表（与 env_v200 逐格对拍验证） | 模型作废（环境另有缺陷） |
| v2.1.6 | 修复移动一步直达最优可达格 | 模型作废（奖励缺陷仍在） |
| v2.1.7 | 奖励恢复对手响应后结算，找回占点信号 | 该次训练因下条缺陷中断 |
| v2.1.8 | 对手动作被拒且游戏已结束时不再崩溃 | 模型作废（交替损坏环境） |
| v2.1.9 | 恢复动作后状态刷新，修复回合交替损坏 | 训练环境修复 |
| v2.2.0 | 座位随机化：智能体坐 player_b 时模型对手满血 | 产出当前推荐模型 |
| v2.2.1 | 训练收尾交付评估 best 断点而非终点模型 | 仅交付逻辑变化 |
| v2.3.0 | 接入对称随机地图；观测 3922→5974 维（331 格标准定序）；归一化随局配置动态化；单位格补攻击力特征 | 旧模型全部不兼容；v2.1.x/v2.2.x 改用 `env_v22.py` + `run_model_v22.py`，54 动作按文件名版本分流 |

服务端 `src/api/bots.ts` 将 512 路由到 `run_model_v100.py`，38 路由到 `run_model_v200.py`；
54 动作按文件名版本分流：≥ v2.3 → `run_model.py`，否则 → `run_model_v22.py`；未知动作空间会被拒绝。
即使两个模型动作数相同，只要动作语义改变，也必须创建新的环境快照和运行器。

## 新增模型时的固定模板

以后新增模型请复制下面的结构，使用新的二级标题，不要只更新共享表格：

```markdown
## 模型 N：<模型文件名去掉 .zip>

### 身份与文件
- 文件、定位、地图、训练版本、训练日期、训练步数、文件大小、参数量、运行器

### 网络、观察和动作
- policy/value 网络、观察维度与编码、动作空间和 mask 语义

### 训练过程与超参数
- 环境、并行度、PPO 参数、设备、checkpoint、评估设置

### 训练对手与策略
- 对手脚本/风格、奖励、模型可能学到的战术和已知偏置

### 性能记录
- TensorBoard 指标、独立评估协议、胜率/回报/据点/动作统计和样本限制

### 部署与兼容性
- 运行器、允许座位、地图限制、能否续训、迁移方式
```

## 共同限制与后续方向

- 当前训练只有一个环境实例，样本吞吐受 TypeScript worker 限制；并行环境或批量引擎是主要加速方向。
- 对手主体仍是手写规则 + 一个固定的 v2.0.0 模型，尚未 self-play；模型可能针对现有对手分布过拟合。
- 观察向量未显式编码完整攻击力、防御、移动范围和据点类型，策略只能从格占用、HP 和全局量间接推断。
- `special` 槽目前主要对应重装爆破；增加特殊技能时必须明确新语义并升版本。
- 训练后期存在胜率震荡（v2.2.0 峰值 90% → 终点 45%），已通过交付 best 断点兜底；后续可探索学习率衰减或早停。
- v2.0.0 只能在 player_a 满血，验收时固定该座位；评估应使用镜像局排除座位/先手因素（`default` 地图实测无先手优势）。
- v2.0 的旧视角限制无法通过后处理修复；可比结果必须使用各自快照运行器并保留兼容性警告。
