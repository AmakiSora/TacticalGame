# RL 模型说明

本文按“一个模型一个大标题”组织。以后新增模型时，应新增一个独立的二级标题，并完整填写该模型的文件、版本、网络、输入输出、训练数据、对手、策略、性能、部署方式和兼容性；不要只在总表里追加一行。

资料来源：`rl/RELEASE_NOTES.md`、`rl/README.md`、`rl/env.py`、`rl/env_v27.py`、`rl/env_v200.py`、`rl/local_env.py`、`rl/train.py`、`rl/extractors.py`、`rl/distill.py`、`rl/evaluate_cross.py`、模型 zip 内的 `data`/`system_info.txt`，以及 `rl/tb/` TensorBoard 日志。性能数字只对注明的评估协议负责，不代表所有地图、座位或真人对手上的泛化能力。

## 模型状态总表

v2.7.0 是 v3.0.2 之前的生产 champion：96 局全新随机图配对换座取得 66:30，95% Wilson 下界 58.9%，两个座位的下界都超过 50%。
v2.8.0 只改训练管线（语义与 v2.7 相同），验收 43:53 未反超，不替换 champion。
v3.0.0（155 分层动作 / 6715 维观测 / Transformer 编码器 / 蒸馏冷启动）验收 51:45 与 v2.7.0 打平，
但把 v2.8 的后手座短板从 29.2% 抬到 47.9%；其交付断点只有 5 万步，因价值头尺度缺陷导致 PPO 前期退化。
v3.0.1 已修复该缺陷（价值目标不再缩放，PPO 初始学习率降到 5e-5）：训练在 749,568 步（25%）被 GPU 驱动错误中断，
但 75 万步内 `explained_variance` 全程 0.80-0.89、对锚点胜率 40%-60% 无趋势性下跌，**崩溃已治好**；
best 为 35 万步断点，验收 96 局对 v2.7.0 **49:47**（51.0%，Wilson 下界 41.2%，先手座 62.5% / 后手座 39.6%），仍为打平。
v3.0.0 与 v3.0.1 两代独立证据表明「蒸馏冷启动 + 少量 PPO」是当前配方上限，不续训、不替换 champion；v2.7.0 继续生产。
v3.0.2（合法集内标签平滑 + `RL_ENT_COEF` 死变量修复 + 探索监控）打破探索死锁后，1.4M 断点在两批全新随机图各 96 局配对换座验收中对 v2.7.0 累计 **123:69**（64.1%，Wilson 下界 **57.1%**；先手座 76.0%/下界 66.6%，后手座 52.1%/下界 42.2%），总分下界达到 v2.7.0 当年晋级线同级证据，**已替换为生产 champion**；v2.7.0 降级为上一代 champion 与自对弈锚点。注意：训练期 best 选择键被 default 场景 OOD 崩塌主导，选中的 10 万步断点（96 局 53:43）弱于 1.4M 断点——交付模型以验收互打实测为准，不以训练期 best 为准。

| 模型文件 | 环境版本 | 状态 | 说明 |
|---|---|---|---|
| `rl/test-output/hex_ppo_v3.0.1_20260904_random_selfplay_350000_best.zip` | v3.0.1 | 与 champion 同水平（**未交付**） | 源自 `rl/checkpoints/random/20260904-143009/best/best_model.zip`（35 万步），已用 `sanitize_delivery_zip` 清掉 lr 闭包。随机图 96 局同图配对对 v2.7.0 **49:47**（51.0%，Wilson 下界 41.2%），先手座 30/48（62.5%）、后手座 19/48（39.6%）。训练在 749,568 步（25%）因 `nvlddmkm` Event 153 GPU 驱动错误中断，交付模型未生成；75 万步内无崩溃（`explained_variance` 0.80-0.89、`ep_rew_mean` 6.6→10.9、target_kl 未触发），证实 v3.0.1 的价值头尺度修复有效 |
| `hex_ppo_v3.0.1_20260904_distilled.zip` | v3.0.1 | 中间产物（勿部署） | v3.0.1 蒸馏冷启动断点：价值头改在未缩放回报上训练 + Huber，5220 样本 12 轮、验证集动作准确率 68.3%，std(V)/std(return) 由 0.16 升到 0.85、平均偏差 −2.56 → −0.91 |
| `rl/models/hex_ppo_v3.0.2_20260905_random_selfplay_1440000.zip` | v3.0.2 | **当前推荐（全地图 champion）** | 1.4M 断点交付（已 sanitize）。对 v2.7.0 两批全新随机图各 96 局累计 **123:69**（64.1%，Wilson 下界 57.1%），先手座 76.0%（下界 66.6%）、后手座 52.1%（下界 42.2%）；批次一 61:35、批次二 62:34，为五代以来首次战胜 v2.7.0 |
| `hex_ppo_v3.0.2_20260905_random_selfplay_1500000.zip` | v3.0.2 | 中间产物（勿部署） | 训练期 best（10 万步断点，已 sanitize）。96 局对 v2.7.0 仅 53:43——best 选择键被 default 场景 OOD 崩塌主导，漏掉了更强的 1.4M 断点 |
| `hex_ppo_v3.0.2_20260905_distilled.zip` | v3.0.2 | 中间产物（勿部署） | 掩码标签平滑（eps=0.15）8 轮蒸馏冷启动断点；验证集动作准确率 67.4%，新候选概率质量 11.52%（v3.0.1 无平滑时 0.34%） |
| `hex_ppo_v3.0.0_20260903_random_selfplay_3000000.zip` | v3.0.0 | 与 champion 同水平（观察中） | 155 分层动作 + 6715 维观测 + hex_transformer；随机图 96 局对 v2.7.0 **51:45**（53.1%，CI [43.2%, 62.8%]），先手座 28:20、**后手座 23:25**（v2.8 仅 14:34）。交付断点实为 5 万步（蒸馏冷启动 + 少量 PPO），后续 PPO 因价值头尺度缺陷退化 |
| `hex_ppo_v3.0.0_20260903_distilled.zip` | v3.0.0 | 中间产物（勿部署） | v2.7.0 老师蒸馏冷启动断点，45220 样本 8 轮、验证集动作准确率 67.9%；价值头因目标被除以 std 而系统性偏小 6 倍（std(V)/std(return)=0.16） |
| `hex_ppo_v2.8.0_20260903_random_selfplay_6000000.zip` | v2.8.0 | 历史（不推荐替换） | 管线版：8 环境 300 fps、后台配对评估、Wilson 选 best、PFSP 自对弈、target_kl 0.02；随机图 96 局对 v2.7.0 **43:53**（下界 35.2%，先手座 29:19、后手座 14:34），同配方已到平台期 |
| `hex_ppo_v2.7.0_20260901_random_selfplay_4000000.zip` | v2.7.0 | 上一代 champion（被 v3.0.2-1.4M 取代；v3.0 自对弈锚点） | 6205 维可观测性修复 + 混合地图 + 配对评估；随机图 96 局胜 v2.4.0 66:30，default 24:0，forge 19:5，dual-lanes 14:10 |
| `hex_ppo_v2.6.0_20260831_random_selfplay_4000000.zip` | v2.6.0 | 历史（不推荐） | 对手生态加压（自对弈 85%/近期加权/锚点 15%）；仍败于 v2.4.0（10:14），且 default 0:24 全崩（后手能力也丢），灾难性遗忘 |
| `hex_ppo_v2.4.0_20260830_random_selfplay_2000000.zip` | v2.4.0 | 历史 champion / v2.7 锚点 | 256×256 大网络；曾连续胜 v2.5/v2.6，现被 v2.7 在随机图 96 局以 66:30 超越 |
| `hex_ppo_v2.3.3_20260830_random_selfplay_2000000.zip` | v2.3.3 | 历史 | 续训+学习率衰减+锚点；被 256 网络的 v2.4.0 3:5 超越 |
| `hex_ppo_v2.3.2_20260829_random_selfplay_800000.zip` | v2.3.2 | 历史 | 首个任意地图可战模型；被 v2.3.3 3:5 小幅超越 |
| `hex_ppo_v2.3.1_20260829_random_modelmix_800000.zip` | v2.3.1 | 历史 | 首个随机地图模型；随机图对 v2.2.0 7:1、对规则对手 14:6；但被自对弈的 v2.3.2 1:7 压制 |
| `hex_ppo_v2.2.0_20260827_default_modelmix_best.zip` | v2.2.0 | 历史 | 对 v2.0.0 真身 16:0，对 v2.1.3 累计 16:2；被 v2.3.2/v2.3.3 在 default 主场 1:7 反超 |
| `hex_ppo_v2.0.0_20260824_default_rule_500000.zip` | v2.0.0 | 基准/历史 | 唯一能在主场满血出战的旧模型；v2.1.x 的模型对手与验收基准都是它 |
| `hex_ppo_v2.1.3_20260826_default_rule_defensive_800000.zip` | v2.1.3 | 历史 | 单一 defensive 对手训练；弱于 v2.0.0、强于 v2.1.4–v2.1.8 |
| `hex_ppo_v2.1.1_20260825_default_rule_mixed_500000.zip` | v2.1.1 | 历史 | 早期 v2.1 模型，跨版本评估中弱于 v2.0.0 |
| `hex_ppo_v2.1.8_20260827_default_modelmix_920000.zip` | v2.1.8 | **作废** | 在回合交替损坏的环境下训练（见模型 9），自评 100% 胜率为虚假数据 |
| `hex_ppo_v2.1.6_20260827_default_modelmix_800000.zip` | v2.1.6 | **作废** | 训练环境仍丢失占点奖励信号，互打全败 |
| `hex_ppo_v2.1.5_20260827_default_modelmix_600000.zip` | v2.1.5 | **作废** | 移动只走一格 + 无占点信号环境下训练，互打全败 |
| `hex_ppo_v2.1.4_20260826_default_modelmix_800000.zip` | v2.1.4 | **作废** | 模型对手动作映射错位，训练对手实为噪声 |
| `hex_ppo_v1.0.0_20260824_default_random_opponent_120000.zip` | v1.0.0 | 历史 | 512 动作，仅由 `run_model_v100.py` 路由 |
| `hex_ppo_v1.0.0_20260824_default_random_opponent_100096.zip` | v1.0.0 | 历史 | 同上，更早的实验模型 |

注意：不存在 v2.1.7 模型文件（该版本训练在约 13 万步处因对手回合规避崩溃而中断，其续训产物即上表的 v2.1.8 文件，同样作废）。

## 模型 1：hex_ppo_v2.2.0_20260827_default_modelmix_best

### 身份与文件

- **文件**：`rl/models/hex_ppo_v2.2.0_20260827_default_modelmix_best.zip`（来源：`rl/checkpoints/default/20260827-182746/best/best_model.zip`）
- **定位**：**当前默认推荐模型**，首个在公平座位下打赢 v2.0.0 真身的模型。
- **训练环境版本**：v2.2.0（2026-08-27），含 v2.1.9 的回合交替修复。
- **地图/对手**：`default` 地图，`modelmix` 对手：50% 概率使用 v2.0.0 模型对手，其余在 aggressive / mixed 规则风格间切换；每局随机分配智能体座位（player_a / player_b 各 50%）。
- **训练步数**：800,000；交付的是训练中评估选出的 best 断点，非终点快照（终点快照对 v2.0.0 仅 0:8，后期震荡退化）。
- **推荐运行器**：`rl/run_model.py`；服务端按动作空间 54 自动路由。

### 网络、观察和动作

网络结构与其余 54 动作模型相同：两个 `3922 → 64 → 64` 的 Tanh MLP（actor/critic 分离），action head 54。观察为 3,922 维（217 格 × 18 特征 + 16 全局特征），相对视角编码。

动作空间 `Discrete(54)`：`end_turn` 1 个、12 个稳定单位槽 × 4 意图（`move`/`attack`/`heal`/`special`）、5 个部署槽。移动候选为离战略目标最近的可达格（BFS 绕路，一步直达，与引擎“一次移动可达任意格”的语义对齐）。

### 训练过程与关键环境语义

- 超参数沿用标准配置（`learning_rate=3e-4`、`n_steps=256`、`batch_size=64`、`n_epochs=10`、`gamma=0.99`、`ent_coef=0.01`、`clip_range=0.2`），CUDA 训练，评估每 10,000 步 × 20 局。
- **奖励在对手响应之后结算**（v2.1.7 起恢复）：占点在回合边界结算，若像 v2.1.0–v2.1.6 那样立即结算，占点收益会被基线更新吸收，模型将不再抢点。
- **座位随机化**（v2.2.0）：智能体坐 player_b 时，v2.0.0 模型对手恰好坐在它的主场 player_a，相对视角编码与其原生编码完全一致，对手即满血真身；坐 player_a 时对手为退化形态。
- 依赖两个更底层的修复，否则训练数据全部失真：
  - 动作后必须刷新状态再进对手回合（引擎只在 `end_turn` 校验回合，陈旧状态会让对手回合被跳过，智能体每回合白嫖 1 个先手动作）；
  - 移动动作必须一步走到最优可达格（引擎允许一步移动任意可达格）。

### 性能记录

评估协议：`rl/evaluate_cross.py`，确定性策略，`default` 地图，15 回合裁决分定胜负。

| 对局 | 结果 |
|---|---|
| vs `v2.0.0`（v2.0.0 坐主场 player_a） | **16:0**（两轮合计 23:1） |
| vs `v2.1.3`（两种座位） | **8:0** / 6:2 |

训练期评估（混合对手、含约半数满血 v2.0.0）：胜率从 35% 起步，中期峰值约 90%，终点回落到 45%——后期震荡正是交付 best 而非终点模型的原因。自 v2.2.1 起训练收尾自动交付 best 断点。

### 部署与兼容性

- 双座位可战（训练覆盖两个座位），`run_model.py` 任意座位均可。
- 仅适用于普通双人顺序地图；`standoff`/`artillery-zone` 等需要单独环境。
- 可在当前环境续训（`RL_LOAD_MODEL` 指向本文件），但继续训练可能重现后期震荡，建议续训时缩短步数或调低学习率。

## 模型 2：hex_ppo_v2.0.0_20260824_default_rule_500000

### 身份与文件

- **文件**：`rl/models/hex_ppo_v2.0.0_20260824_default_rule_500000.zip`
- **定位**：**历史基准模型**。v2.1.x 系列的训练对手与验收标尺；保留用于旧版本大厅和跨版本离线对战。
- **训练环境版本**：v2.0.0（2026-08-24）。首个规则对手 + 固定意图槽位版本。
- **地图/对手**：`default` 地图，单一 `rule` 对手。
- **训练步数**：目标 500,000；`num_timesteps=500,224`。参数量 512,999；约 6.26 MB。
- **推荐运行器**：`rl/run_model_v200.py`；必须搭配不可变环境快照 `rl/env_v200.py`。

### 网络、观察和动作

两个 `3922 → 64 → 64` 的 Tanh MLP；action head 38。观察 3,922 维，但当时编码**固定以 `player_a` 为己方视角**。

动作空间 `Discrete(38)`：`end_turn` 1 个、8 个单位槽 × 4 意图 32 个、5 个部署槽。单位每步按（兵种, q, r, -hp）排序取前 8 分槽（槽位跨状态不稳定）；移动要求严格接近目标（不绕路）。这两点语义差异是“旧模型对手”必须用 `legacy` 动作表复现的原因。

### 训练过程与对手

MaskablePPO 标准超参数；本地 TypeScript worker；规则对手优先级：攻击 → 治疗 → 补给 ≥90 部署 → 移动。奖励在对手响应后结算（该时机后被证明是正确的，v2.1.7 恢复）。

### 性能记录

无周期评估标量，仅 rollout 日志（平均回报 -7.60 → 20.76）。跨版本评估（`evaluate_cross.py`，确定性，15 回合裁决）：对 v2.1.1–v2.1.8 各版本保持压制（见各模型条目）；镜像对局证明 `default` 地图无先手优势。重要限制：**只能坐在 player_a 满血**——坐 player_b 时固定视角编码严重分布外（实测训练形态被真身 8:0）。

### 部署与兼容性

必须使用 `run_model_v200.py` + `env_v200.py`，尽量固定 player_a。38 动作不能加载进 54 动作环境；作为训练对手时由 `env.py` 的 `legacy` 分支驱动（38 动作语义 + 相对视角观测）。

## 模型 3：hex_ppo_v2.1.3_20260826_default_rule_defensive_800000

### 身份与文件

- **文件**：`rl/models/hex_ppo_v2.1.3_20260826_default_rule_defensive_800000.zip`
- **定位**：历史模型；v2.1.4–v2.1.8 时期的对比标尺。
- **训练环境版本**：v2.1.3（2026-08-26，仅文件命名规范变更）。
- **地图/对手**：`default` 地图，**单一 `defensive` 规则对手**（文件名 `rule_defensive`；注意不是 mixed），800,000 步。
- **推荐运行器**：`rl/run_model.py`（54 动作）。

### 已知问题与性能

该模型训练于移动只走一格、奖励无占点信号的 v2.1 环境（见模型 8），棋力上限有限，但在互打中稳定强于 v2.1.4–v2.1.8（对比 8:0 / 14:0），弱于 v2.0.0（两种座位均 0:4 以上）。它对单一对手脚本过拟合的风险较高，与 modelmix 系列的横向对比需注意训练条件不同。

## 模型 4：hex_ppo_v2.1.1_20260825_default_rule_mixed_500000

### 身份与文件

- **文件**：`rl/models/hex_ppo_v2.1.1_20260825_default_rule_mixed_500000.zip`
- **定位**：早期 v2.1 历史模型，非部署模型。
- **训练环境版本**：v2.1.1（2026-08-25），mixed 规则对手，500,000 步。参数量 514,039。
- **推荐运行器**：`rl/run_model.py`。

### 性能记录

训练期评估（20 局/次，mixed 规则对手）：平均胜率全程均值 38.5%、单点最高 70%；平均据点 2.09。跨版本抽样中固定坐 player_b 时对 v2.0.0 0:4。与后续版本一样受“移动一格 + 无占点信号”环境缺陷影响，不应以当时自评数字推断真实棋力。

## 模型 5：hex_ppo_v1.0.0_20260824_default_random_opponent_120000

### 身份与文件

- **文件**：`rl/models/hex_ppo_v1.0.0_20260824_default_random_opponent_120000.zip`
- **定位**：v1 训练流程的默认图历史模型；不是当前部署模型。
- **训练步数**：`num_timesteps=120,000`；约 6.63 MB；参数量 543,809。
- **环境特征**：观察 3,922 维，动作 `Discrete(512)`；动作索引表示旧环境合法动作列表中的第 N 项。
- **训练设备记录**：PyTorch 2.13.0 CPU（zip 生成时的历史信息）。

### 网络与动作

两个 `3922 → 64 → 64` Tanh MLP，action head `512 × 64 + 512`。512 个动作不是稳定意图槽；每个状态重新生成合法动作列表，由 mask 屏蔽不可用索引，不能解释为固定语义。

### 兼容性

`Discrete(512)` 与 38/54 完全不兼容；服务端路由到 `run_model_v100.py` + `env_v100.py` 快照。随机对手训练，无统一胜率评估。

## 模型 6：hex_ppo_v1.0.0_20260824_default_random_opponent_100096

### 身份与文件

- **文件**：`rl/models/hex_ppo_v1.0.0_20260824_default_random_opponent_100096.zip`
- **定位**：更早的 v1 随机对手实验模型；`num_timesteps=100,096`；参数量 543,809。
- **观察/动作**：同模型 5（3,922 维 / `Discrete(512)`）。
- 仓库没有统一胜率评估，不能按步数推断与模型 5 的强弱；适合作旧环境回归样本。由 `run_model_v100.py` 路由。

## 模型 7：v2.1.4 / v2.1.5 / v2.1.6（作废批次）

三个模型文件：

- `hex_ppo_v2.1.4_20260826_default_modelmix_800000.zip`
- `hex_ppo_v2.1.5_20260827_default_modelmix_600000.zip`
- `hex_ppo_v2.1.6_20260827_default_modelmix_800000.zip`

**状态：全部作废，保留仅作历史对照。** 它们分别训练于三个被后续证实有缺陷的环境：

| 版本 | 环境缺陷（事后确认） |
|---|---|
| v2.1.4 | 模型对手 38 动作按下标硬映射到 54 动作表，槽位与移动语义错位，对手实为噪声 |
| v2.1.5 | 移动动作只走一格（引擎本可一步直达），机动效率 1/3–1/5，连带抑制部署 |
| v2.1.6 | 移动已修，但奖励立即结算吞掉了占点收益（权重最大项 90/点），策略不抢点 |

互打成绩（`evaluate_cross.py`，8 局/组）：三者对 v2.0.0、对 v2.1.3 均 0:8，换座位同样全败。这些模型的自评胜率不能作为棋力依据。

## 模型 8：hex_ppo_v2.1.8_20260827_default_modelmix_920000（作废）

### 身份与文件

- **文件**：`rl/models/hex_ppo_v2.1.8_20260827_default_modelmix_920000.zip`（v2.1.7 训练中断后从 12 万步断点续训的产物；不存在独立的 v2.1.7 模型文件）
- **状态**：**作废**。

### 作废原因：回合交替损坏

v2.1.7 改动误删了“智能体动作后、对手阶段前”的状态刷新：对手回合循环读到陈旧状态被跳过；而引擎只在 `end_turn` 时校验回合归属，智能体的单位动作在对手回合照样执行——**每回合白嫖 1 个先手动作并偷走对手 1 点 AP**。该环境下训练自评 100% 胜率，换到正确交替的互打环境立刻 0:8（对 v2.0.0）/ 0:14（对 v2.1.3，两种座位）。

教训已写入 `rl/RELEASE_NOTES.md` v2.1.9 条目：训练自评胜率异常时必须先做交叉验证。

## 模型 9：hex_ppo_v2.3.1_20260829_random_modelmix_800000

### 身份与文件

- **文件**：`rl/models/hex_ppo_v2.3.1_20260829_random_modelmix_800000.zip`（来源：`rl/checkpoints/random/20260829-055121/best/best_model.zip`）
- **定位**：**随机地图推荐模型**，首个在对称随机地图上训练的模型；静态图仍由 v2.2.0 担任。
- **训练环境版本**：v2.3.1（2026-08-29），含并行训练支持。
- **地图/对手**：每局一张对称随机地图（半径 6-10，地形/复活点/据点/回合数 11-25/行动点 3-7/兵种数值/经济与总部全部随机，地图种子从环境随机源派生），`mixed` 规则对手（随机图上模型对手默认关闭）；每局随机分配智能体座位。
- **训练步数**：目标 800,000；交付的是约 7 万步处的评估 best 断点（`num_timesteps=70000`）——胜率峰值 75% 出现在训练早期，其后在 15%-75% 震荡未再刷新，与 v2.2.0 的后期震荡同源但更早更剧烈（每局地图不同加剧方差）。参数量 776,695；约 9.56 MB。
- **推荐运行器**：`rl/run_model.py`；服务端按 54 动作 + 文件名版本 ≥ v2.3 路由。

### 网络、观察和动作

两个 `5974 → 64 → 64` 的 Tanh MLP（actor/critic 分离），action head 54。观察 5,974 维（331 格半径 10 标准定序 × 18 特征 + 16 全局特征），相对视角编码；归一化除数读本局 `config.balance`（回合数/行动点/起始补给/总部 HP 均随局变化），单位格含攻击力特征。动作空间同 v2.2.0：`Discrete(54)`，12 稳定槽×4 意图 + 5 部署槽。

### 训练过程与超参数

- 首个用 `RL_NUM_ENVS=4` 的 `SubprocVecEnv` 并行训练（每环境一个独立引擎 worker，样本吞吐约 4 倍）；CUDA，其余超参数沿用标准配置（`learning_rate=3e-4`、`n_steps=256`、`batch_size=64`、`n_epochs=10`），评估每 10,000 帧 × 20 局（对手即 mixed 规则）。
- 随机地图上的评估方差天然更大：每次评估都是未见过的新地图，胜率曲线在 15%-75% 大幅震荡，best 断点选择也因此偏向早期峰值。
- 奖励在对手响应后结算、移动一步直达等语义继承 v2.2.x，无变化。

### 性能记录

评估协议：确定性策略，双座位（`--swap-sides` 或环境随机分配），各模型用各自版本的编码（`evaluate_cross.py` 按观测维度路由）。

| 对局 | 结果 |
|---|---|
| vs mixed 规则对手（对称随机图，20 局，座位随机分配 9a/11b） | **14:0:6**，平均据点 1.75，平均奖励 +19.35 |
| vs `v2.2.0`（随机图，8 局换边） | **7:1** |
| vs `v2.2.0`（default 静态图，8 局换边） | **0:8** |
| 训练期评估（随机图、规则对手） | 峰值 75%@约 7 万步，区间 15%-75%，终点 55% |

default 上 0:8 属预期分布外：训练全程未见 default 地图；同时说明随机地图训练未能泛化到静态图的固定布局，两个模型目前是地图专属关系而非替代关系。

### 部署与兼容性

- 双座位可战，`run_model.py` 任意座位均可；适合随机地图对局与随机地图验证。
- 不建议用于 default 等静态地图（实测 0:8）；静态图仍用 v2.2.0。
- 可在当前环境续训（`RL_LOAD_MODEL` 指向本文件或 `rl/checkpoints/random/20260829-055121/` 内断点）；单纯续训大概率继续震荡，改进方向见「共同限制」。
- 3922 维的 v2.1/v2.2 模型不能加载进本环境；跨版本对战用 `evaluate_cross.py`。

## 模型 10：hex_ppo_v2.3.2_20260829_random_selfplay_800000

### 身份与文件

- **文件**：`rl/models/hex_ppo_v2.3.2_20260829_random_selfplay_800000.zip`（来源：`rl/checkpoints/random/20260829-082950/best/best_model.zip`）
- **定位**：**当前推荐模型，首个任意地图可战模型**：随机图主场统治，未见过的 default 验证集也 7:1。
- **训练环境版本**：v2.3.2（2026-08-29），新增自对弈快照阶梯对手。
- **地图/对手**：每局一张对称随机地图（同 v2.3.1）；对手 60% 从自己历史策略快照（最近 8 个）中随机抽，40% mixed 规则；座位每局随机。
- **训练步数**：目标 800,000；交付约 34 万步处的评估 best 断点（`num_timesteps=340000`，综合分（胜率，据点，奖励）最优）。胜率曲线仍在 15%-75% 震荡（随机地图评估的固有大方差），峰值 75%@5 万步。参数量 776,695。
- **推荐运行器**：`rl/run_model.py`；服务端按 54 动作 + 文件名版本 ≥ v2.3 路由。

### 网络、观察和动作 / 训练过程与超参数

同 v2.3.1（5974→64→64 Tanh MLP×2，Discrete(54)，相对视角编码）；4 环境并行、CUDA、标准超参；另每 5000 次回调存一个自对弈快照（保留最近 20 个）。评估环境仍为纯规则对手，标尺与历史可比。

### 性能记录

评估协议：确定性策略，双座位换边。

| 对局 | 结果 |
|---|---|
| vs mixed 规则对手（随机图，20 局） | 12:0:8，平均据点 1.70（对规则未提升，但规则对手不再是有效标尺） |
| vs `v2.3.1`（随机图，8 局换边） | **7:1** |
| vs `v2.2.0`（随机图，8 局换边） | **7:1** |
| vs `v2.2.0`（**default 验证集**，8 局换边） | **7:1**（v2.3.1 同一对局为 0:8） |
| 训练期评估（随机图、规则对手） | 区间 15%-75%，峰值 75%@5 万步 |

default 验证集从 0:8 翻到 7:1 证实：布局泛化本就存在，瓶颈确实是对手强度；自对弈提升绝对棋力后，未见过的地图也能赢——即「无论玩什么图都得胜利」的目标初步达成。
对规则对手胜率反降（14:6→12:8）不是退化：自对弈后策略针对的是会反击的对手，对不会适应的弱规则对手的刷分不再优先。
训练期胜率曲线震荡但互打分持续上升，再次说明随机地图上的训练自评胜率不是可靠棋力标尺，交付/验收必须用互打。

### 部署与兼容性

- 双座位可战，任意标准双人地图（随机图与静态图）推荐都用本模型。
- 可在当前环境续训；快照目录 `rl/selfplay/random` 已有梯度，续训即恢复自对弈。
- 3922 维的旧模型不能加载进本环境；跨版本对战用 `evaluate_cross.py`。

## 模型 11：hex_ppo_v2.3.3_20260830_random_selfplay_2000000

### 身份与文件

- **文件**：`rl/models/hex_ppo_v2.3.3_20260830_random_selfplay_2000000.zip`（来源：`rl/checkpoints/random/20260829-211534/best/best_model.zip`）
- **定位**：**当前推荐模型（全地图）**，v2.3.2 的续训产物；文件名 2000000 是目标步数，实际 `num_timesteps=770000`。
- **训练环境版本**：v2.3.3（2026-08-29 晚），首次启用学习率线性衰减（3e-4→3e-5）与锚点对手（v2.3.2 自身）。
- **训练过程**：从 v2.3.2 交付断点（34 万步）续训，目标 200 万步；**训练在约 122 万步处进程被外力中断**（日志无错误尾巴，疑似终端/宿主机休眠断开），断点完好。交付评估 best（77 万步处，40 局评估峰值 77.5%）。
- **推荐运行器**：`rl/run_model.py`；服务端按 54 动作 + 文件名版本 ≥ v2.3 路由。

### 网络、观察和动作 / 训练过程与超参数

同 v2.3.2（64×64 Tanh MLP×2，Discrete(54)，5974 维相对视角）；4 环境并行、CUDA；新增：学习率线性衰减、锚点对手（续训模型自身）、快照按保存时间排序。评估 40 局/次（降噪）。

### 性能记录

评估协议：确定性策略，双座位换边。

| 对局 | 结果 |
|---|---|
| vs mixed 规则对手（随机图，20 局） | **15:0:5**，平均据点 1.95（三代最高） |
| vs `v2.3.2`（随机图，8 局换边） | **5:3** |
| vs `v2.2.0`（随机图，8 局换边） | **7:1** |
| vs `v2.2.0`（**default 验证集**，8 局换边） | **7:1** |
| 训练期评估（随机图、规则对手，40 局） | 区间 35%-77.5%，峰值 77.5%@77 万步 |

对 v2.3.2 仅 5:3 且训练后半段评估未再超越 77 万步峰值：续训 + 学习率衰减带来小幅提升但未拉开差距，后期震荡依旧（衰减把振幅收窄了一些，未消除）。续训的 88 万新帧主要贡献是稳定性而非棋力上限。

### 部署与兼容性

- 双座位可战，任意标准双人地图推荐都用本模型（已具备部署条件，服务器当前仍是 v2.3.2）。
- 断点 `rl/checkpoints/random/20260829-211534/` 完好（至 122 万步），可随时补训剩余约 78 万步；
  但基于 5:3 的边际提升，补训的收益预期有限。
- 教训：长时间后台训练需防宿主机休眠/终端断开；分段续训或部署守护更稳。
- 3922 维的旧模型不能加载进本环境；跨版本对战用 `evaluate_cross.py`。

## 模型 12：hex_ppo_v2.4.0_20260830_random_selfplay_2000000

### 身份与文件

- **文件**：`rl/models/hex_ppo_v2.4.0_20260830_random_selfplay_2000000.zip`（来源：`rl/checkpoints/random/20260830-015922/best/best_model.zip`，交付时已自动清洗学习率闭包）
- **定位**：**当前推荐模型（全地图）**；参数量 3,204,919（约为 64 网络模型的 4 倍），文件 22.8 MB。
- **训练环境版本**：v2.4.0（2026-08-30），首次 256×256 网络；从零训练（大网络与旧断点不兼容）。
- **地图/对手**：每局一张对称随机地图；对手 60% 自对弈（锚点 v2.3.3 + 快照阶梯，按保存时间取最近 8 个），40% mixed 规则；座位每局随机。
- **训练步数**：目标 2,000,000 完整跑完；交付 196 万步处的评估 best（`num_timesteps=1960000`，综合分最优）——与前三代 best 偏早期不同，大网络收敛更慢、峰值出现在训练后期。
- **推荐运行器**：`rl/run_model.py`；服务端按 54 动作 + 文件名版本 ≥ v2.3 路由，无需改动。
- 首次启动曾于约 3 分钟处因中文 Windows 代码页 936 传染、GBK 输出解码崩溃（已修复：管道解码容错 + 启动器固化 chcp 65001），重跑后完整跑完。

### 网络、观察和动作 / 训练过程与超参数

**256×256 Tanh MLP×2**（`RL_NET_WIDTH=256`），Discrete(54)，5974 维相对视角编码；4 环境并行、CUDA、`n_steps=512`/`batch=256`（大缓冲）、快照每 10 次回调（约 2 万帧）、评估 40 局/次；学习率 3e-4→3e-5 线性衰减；锚点对手为 v2.3.3。

### 性能记录

评估协议：确定性策略，双座位换边。

| 对局 | 结果 |
|---|---|
| vs mixed 规则对手（随机图，20 局） | 14:0:6，平均据点 **2.20**（历代最高） |
| vs `v2.3.3`（随机图，8 局换边） | **5:3** |
| vs `v2.2.0`（随机图，8 局换边） | 5:3 |
| vs `v2.2.0`（**default 验证集**，8 局换边） | 6:2 |
| 训练期评估（随机图、规则对手，40 局，200 点） | 区间 32.5%-77.5%，综合 best@196 万步 |

代际互打保持优势（每代 5:3），但优势幅度没有随网络容量拉大；对 v2.2.0 的比分（6:2 / 5:3）略低于 v2.3.3 时期（7:1 / 7:1），8 局样本下差距在噪声范围内。据此判断：当前 64→256 的容量升级带来的边际收益已接近饱和，棋力上限更可能受限于对手生态（规则对手+自身快照）与观测表达，而非网络容量。
对规则胜率持平但据点数持续上升（1.70→1.95→2.20）：控制地图的能力在稳步变强。

### 部署与兼容性

- 双座位可战，任意标准双人地图推荐都用本模型；服务器当前仍是 v2.3.3，可部署替换。
- 256 网络与 64 网络断点互不兼容；可在当前环境续训（断点 `rl/checkpoints/random/20260830-015922/` 至 200 万步）。
- 3922 维的旧模型不能加载进本环境；跨版本对战用 `evaluate_cross.py`。

## 模型 13：hex_ppo_v2.5.0_20260830_random_selfplay_2000000

### 身份与文件

- **文件**：`rl/models/hex_ppo_v2.5.0_20260830_random_selfplay_2000000.zip`（来源：`rl/checkpoints/random/20260830-123944/` 的 200 万步终点断点，交付前手工清洗学习率闭包）
- **定位**：与 v2.4.0 统计上同档（见性能记录的复测修正）；参数量 3,230,519，文件 21.9 MB。
- **训练环境版本**：v2.5.0（2026-08-30），观测 5974→6024 维（尾部追加对手上一回合动作历史）；从零训练。
- **地图/对手**：每局一张对称随机地图；对手 60% 自对弈（锚点 v2.4.0 走 env_v24 跨维度编码 + 版本隔离快照阶梯），40% mixed 规则；座位每局随机。
- **训练步数**：目标 2,000,000 完整跑完。**评估 best（6 万步，峰值 80%）互打平庸（对 v2.4.0 4:4、对规则 10:10），不作交付；改交付 200 万步终点断点**（对 v2.4.0 6:2）——训练期评估 best 与真实互打背离最严重的一代。
- **推荐运行器**：`rl/run_model.py`（v2.5 路由）。

### 网络、观察和动作 / 训练过程与超参数

**256×256 Tanh MLP×2**，Discrete(54)，**6024 维相对视角编码**（对手上一回合动作 6 槽×8 维 + 2 维汇总，源自对局事件日志）；4 环境并行、CUDA、`n_steps=512`/`batch=256`、快照每 10 次回调、评估 40 局/次；学习率 3e-4→3e-5 线性衰减；锚点对手为 v2.4.0。

### 性能记录

评估协议：确定性策略，双座位换边。以下为**终点断点（200 万步）**成绩：

| 对局 | 结果 |
|---|---|
| vs mixed 规则对手（随机图，20 局） | 14:0:6，平均据点 1.95 |
| vs `v2.4.0`（随机图，8 局换边） | **6:2**（首次打破历代 5:3 的代际幅度） |
| vs `v2.2.0`（随机图，8 局换边） | **8:0**（历代最强碾压） |
| vs `v2.2.0`（**default 验证集**，8 局换边） | **1:6** ⚠️ 退化（v2.4.0 为 6:2，v2.3.3 为 7:1） |
| 训练期评估（随机图、规则对手，40 局） | 峰值 80%@6 万步，此后 47.5%-72.5% 震荡 |
| 评估 best（6 万步）交叉验证 | 对规则 10:10、对 v2.4.0 4:4、对 v2.2.0 两组 4:4——不能代表真实水平 |

结论：对手动作历史信号在随机图上确实带来棋力提升（代际互打首次拉开到 6:2、对 v2.2.0 8:0），但静态图迁移能力断裂：历代随机图训练都能迁移到 default（7:1→6:2），本代降到 1:6。原因待查：新观测下静态图与随机图的对手动作分布差异、或单次训练方差；在查清前不作全地图推荐。

**⚠ 后续排查修正（同日，以上单次 8 局结论被推翻）**：
- **default 先手崩溃是历代随机图模型通病，非 v2.5.0 独有**：拆座位复测，v2.3.3/v2.4.0/v2.5.0 坐 player_a 对 v2.2.0 全部 0:8；坐 player_b 才能赢（v2.5.0 后手 5:1+1、6:1+1；v2.4.0 后手 4:4/7:1）。历史上记录的 default 7:1/6:2 是换边 8 局小样本下的噪声，不可复现（v2.4.0 复测 16 局仅 9:7）。
- **互打方差极高**：同一配置重复 8 局换边，v2.5.0 vs v2.4.0 随机图结果 6:2/5:3/4:4；v2.5.0 vs v2.2.0 default 后手 6:1+1 与 1:7 交替出现。单次 8 局无统计意义。
- **累计样本归一**：v2.5.0 vs v2.4.0 随机图 24 局累计 14:10（含代际先手对称），两者实际同档；“6:2 历代最强”“8:0 碾压”“1:6 退化”均为小样本假象。
- 模型 13 最终定位修正为：与 v2.4.0 统计同档，部署无需替换；对手动作历史的价值未被证实也未被证伪，需要更大样本评估体系才能判定。

**⚠⚠ 再次修正（评估器 bug 定位后，上条“同档”结论也被推翻）**：
- **evaluate_cross 的 `--swap-sides` 存在座位 bug**：第 2 局换边后重建控制器时固定取初始名字，
  导致第 3 局起座位不再往返（model-a 永远坐 player_b）。**历史上所有 --swap-sides 多局成绩全部作废**，
  包括上条修正里的“24 局累计 14:10”（实为 1 局换边 + 23 局固定座位）。已修复（按当前座位映射轮换）并加注释。
- **修复后真换边复测（各 24 局，分座位）**：
  - 随机图 v2.5.0 vs v2.4.0：**9:15**，v2.5.0 两个座位均败（player_a 2:10、player_b 5:7）。
  - default v2.5.0 vs v2.2.0：总 9:14+1 平，但先手 **0:12**、后手 **9:3**——先手崩确认是系统性的，非噪声。
- **模型 13 最终定位（定案）**：对手动作历史观测未带来棋力提升，反而小幅退步；
  不作任何场景推荐，服务器保持 v2.4.0。此前“历代最强/同档”结论均由座位 bug + 小样本方差叠加产生。

### 部署与兼容性

- 不作推荐（真换边复测被 v2.4.0 15:9 压制）；服务器保持 v2.4.0。
- 6024 维与 5974 维断点互不兼容；可在当前环境续训（断点 `rl/checkpoints/random/20260830-123944/`）。
- v2.3/v2.4 模型由 `run_model_v24.py` 路由；本模型由 `run_model_v25.py` 路由（6024 维专用）。

## 模型 14：hex_ppo_v2.6.0_20260831_random_selfplay_4000000

### 身份与文件

- **文件**：`rl/models/hex_ppo_v2.6.0_20260831_random_selfplay_4000000.zip`（来源：`rl/checkpoints/random/20260831-034422/` 的 356 万步评估 best，交付时已自动清洗学习率闭包）
- **定位**：**不推荐**：对手生态加压未超越 v2.4.0，且静态图能力灾难性遗忘。参数量约 320 万，文件 22.6 MB。
- **训练环境版本**：v2.6.0（2026-08-31）：观测回退 5974 维（v2.5 动作历史被证伪后回退）；自对弈 60%→85%、快照近期加权（几何权重）、锚点 v2.4.0 按 15% 固定出场；从零训练。
- **地图/对手**：每局一张对称随机地图（静态图维持纯验证集，不混入）；对手 85% 自对弈（无快照时锚点当老师）+ 15% mixed 规则；座位每局随机。
- **训练步数**：目标 4,000,000 完整跑完；交付 356 万步处的评估 best（峰值 85%，历代最高，仍出现在后期）。
- **推荐运行器**：`rl/run_model.py`（v2.6 路由）。

### 性能记录（新协议：真换边 24 局累计 + 分座位）

| 对局 | 结果 |
|---|---|
| vs mixed 规则对手（随机图，20 局） | 13:0:7，平均据点 1.55（自对弈 85% 下弱对手成绩降属预期） |
| vs `v2.4.0`（随机图，24 局真换边） | **10:14**（先手座 7:5、后手座 3:7）——仍未超越 |
| vs `v2.2.0`（**default 验证集**，24 局真换边） | **0:24**（先手 0:12、**后手 0:12**）⚠️ 连历代能赢的后手局也全丢 |
| vs `v2.4.0`（随机图，24 局真换边，**终点断点 400 万步**） | **12:12**（先手座 7:5、后手座 5:7）——比 best 的 10:14 好，训练仍在缓慢进步但仍未超越 |
| 训练期评估（随机图、规则对手，40 局） | 峰值 85%@356 万步（历代最高），但与真实互打背离更严重 |
| 对照：v2.4.0 对 v2.2.0 default（8 局） | 3:5（先手 0:4、后手 3:1），后手能力仍在 |

结论：对手生态加压（85% 自对弈 + 近期加权 + 强锚点）**未能突破 v2.4.0**，且高自对弈比例引发静态图灾难性遗忘（v2.5.0 后手尚能 9:3，本代后手 0:12）。训练期评估峰值 85% 是规则对手与自对弈池强度差拉大后的虚高。终点断点（400 万步）对 v2.4.0 12:12 优于 best 的 10:14，证明训练仍在缓慢进步，但进步速率（400 万步仅从落后追到持平）不足以在合理成本内反超，且继续训练只会加深静态图遗忘。当前配方（随机图 + 自对弈阶梯）的天花板已由两轮实验确认：无论观测扩展还是生态加压，都无法在 24 局样本下超越 v2.4.0。

### 部署与兼容性

- 不作推荐；服务器保持 v2.4.0。
- 5974 维观测与 v2.3/v2.4 编码逐格一致；可在当前环境续训（断点 `rl/checkpoints/random/20260831-034422/`）。

## 模型 15：hex_ppo_v2.7.0_20260901_random_selfplay_4000000

### 身份与训练

- **文件**：`rl/models/hex_ppo_v2.7.0_20260901_random_selfplay_4000000.zip`；来源为训练期配对评估 best，模型内 `num_timesteps=3,950,000`，约 24.2 MB。
- **定位**：**当前推荐模型（全地图）**，首个在大样本随机图互打中以 95% 置信下界确认超越 v2.4.0 的模型。
- **网络**：MaskablePPO，策略/价值网络各 256×256 Tanh，54 动作，6205 维观测。
- **观测**：5974 维棋盘/全局状态 + 12×15 稳定单位槽 + 51 维兵种、经济、总部与裁决规则；动作槽对应的具体单位和每局随机规则均可直接观察。
- **地图**：random 70%、default 15%、dual-lanes 7.5%、forge 7.5%；每局随机座位。
- **对手**：70% 自对弈，其中 40% 固定 v2.4.0 champion、60% 均匀历史快照；其余 30% 规则对手。历史池取最近 16 个快照。
- **训练**：400 万目标帧完整跑完，4 环境并行，`n_steps=512`、`batch_size=256`、学习率 3e-4→3e-5；评估每 5 万帧、每场景 20 局固定种子配对换座。

### 性能记录

| 对局 | 结果 |
|---|---|
| vs v2.4.0，随机图，96 局、48 个全新种子成对换座 | **66:30（68.8%）**；95% Wilson CI **58.9%–77.1%** |
| 随机图分座位 | player_a **35:13**（下界 59.0%）；player_b **31:17**（下界 50.4%） |
| vs v2.4.0，default，24 局换座 | **24:0**，两个座位均 12:0 |
| vs v2.4.0，forge，24 局换座 | **19:5** |
| vs v2.4.0，dual-lanes，24 局换座 | **14:10**；player_a 3:9、player_b 11:1，存在明显座位效应 |
| 训练期交付点（395 万帧） | 三场景最弱胜率 85%，平均胜率 95%，平均据点 2.57 |

随机图大样本和两个座位均达到预设晋级条件，default 灾难性遗忘也已消除。dual-lanes 总比分没有退化，但两代模型都呈现显著座位效应；该问题不能靠继续训练当前 54 动作空间可靠解决，应在下一代可选移动落点/目标动作空间中重点验收。

### 部署与兼容性

- 服务端按文件名 v2.7 路由到当前 `run_model.py`；前端模型列表按修改时间排序，本模型默认排在首位。
- v2.7 的 6205 维断点不能由 v2.6 或更早环境加载；v2.6 模型继续走 `run_model_v26.py`。
- 完整 checkpoint 位于 `rl/checkpoints/random/20260901-045740/`，交付 best 位于其 `best/best_model.zip`。

## 模型 16：hex_ppo_v3.0.2_20260905_random_selfplay_1440000

### 身份与文件

- **文件**：`rl/models/hex_ppo_v3.0.2_20260905_random_selfplay_1440000.zip`（来源：`rl/checkpoints/random/20260905-011908/` 的 1,440,000 步断点，交付前已用 `sanitize_delivery_zip` 清洗 lr 闭包，替换为常数 lr 1e-5）
- **定位**：**当前推荐模型（全地图 champion）**，首个在验收互打中战胜 v2.7.0 的模型。
- **训练环境版本**：v3.0.2（2026-09-05）。观测/动作/奖励语义与 v3.0.0 完全相同（6715 维 / Discrete(155)），只改训练配方，无需新快照。
- **训练配方**：v2.7.0 老师蒸馏冷启动（合法集内标签平滑 eps=0.15、8 轮，验证集动作准确率 67.4%）→ PPO 150 万帧。8 环境 SubprocVecEnv、`hex_transformer` 编码器、lr 5e-5→1e-5 线性衰减、`ent_coef=0.03`（经 `resume_custom_objects` 修复后首次真正生效）、锚点 v2.7.0 自对弈局 40% 出场、PFSP 快照阶梯、混合地图（random 70% / default 15% / dual-lanes 7.5% / forge 7.5%）。
- **训练步数**：目标 150 万帧完整跑完（`total_timesteps=1,503,232`，约 4.1 小时 @103-160 fps）；交付 1,440,000 步断点（zip 内 `num_timesteps=1440000`）。
- **推荐运行器**：`rl/run_model.py`；服务端 155 动作路由不变。

### 网络、观察和动作

同 v3.0.0/v3.0.1：`hex_transformer` 棋盘编码器，观测 6715 维（前 6205 维同 v2.7 + 尾部 510 维候选描述），动作 `Discrete(155)`：12 单位槽 ×（7 移动候选 + 3 攻击候选 + 治疗 + 特殊）+ 10 部署候选。

### 训练过程与探索

v3.0.2 的目标是打破 v3.0.1 诊断出的探索死锁（155 动作在行为上退化为 54 动作）。结果：

- **探索确实打开**：`explore/new_candidate_rate` 从 v3.0.1 的 0.00% 升至 0.6%-1.5%（峰值 @30 万步），attack 新候选一度 10%；策略熵全程 1.5-1.8 nats 不再坍缩（v3.0.1 为 0.94→0.78 单调下降）；`explained_variance` 0.63-0.87，target_kl 未触发。
- **但训练期 15 次评估的 best 一直是 10 万步断点**：综合键（最弱场景 Wilson 下界）被 default_champion 场景主导——该场景从 73%@10 万步崩到 18%-35%（后期），先手座多次 0%（先手/后手在 0%↔94% 间摆动，default 开局呈双稳态）；而后期对锚点的 random_champion 60%-70% 走强被掩盖。
- **教训：训练期 best ≠ 最强断点。** 交付 best（10 万步）96 局仅 53:43，1.4M 断点两批累计 123:69。当评估场景含训练分布外（default 对本代训练分布是静态图）时，min_wilson_lb 会系统性偏向早期断点；候选断点交付前必须互打验收。

### 性能记录

验收协议：`rl/evaluate_cross.py --map random --swap-sides --games 96 --seed-prefix <prefix>`，确定性策略，对手 v2.7.0，48 对全新随机图每对配对换座。批次一 `cross` 与 v3.0.0/v3.0.1 验收同一批地图（直接可比），批次二 `cross2` 为全新地图；逐局累计于 `rl/test-output/stats_v302_1400k_v270_random.jsonl`。

| 对局 | 结果 |
|---|---|
| vs v2.7.0，批次一（96 局） | **61:35**（63.5%，下界 53.6%）；先手 72.9% / 后手 54.2% |
| vs v2.7.0，批次二（96 局） | **62:34**（64.6%，下界 54.6%）；先手 79.2% / 后手 50.0% |
| **累计（192 局）** | **123:69**（64.1%，Wilson 下界 **57.1%**）；先手 73/96 = 76.0%（下界 66.6%）、后手 50/96 = 52.1%（下界 42.2%） |
| 同批地图历史对照（批次一） | v2.8.0 43:53 → v3.0.0 51:45 → v3.0.1 49:47 → **本模型 61:35** |

晋级判据说明：总分下界 57.1% 与 v2.7.0 当年晋级证据（66:30、下界 58.9%）同级；先手座下界 66.6% 过线；后手座点估计 52.1% 为历代首次对 v2.7.0 不输（此前 v2.8.0 29.2%、v3.0.0 47.9%、v3.0.1 39.6%），但 96 局样本的下界 42.2% 未过 50%——按「两座位下界均 >50%」的字面标准，后手座还需更大样本确认。

### 部署与兼容性

- 服务端 155 动作 → `run_model.py` 路由不变；前端模型列表按修改时间排序，本模型默认首位。
- 同代中间产物：`hex_ppo_v3.0.2_20260905_random_selfplay_1500000.zip`（训练期 best = 10 万步断点，勿部署）、`hex_ppo_v3.0.2_20260905_distilled.zip`（蒸馏冷启动断点，勿部署）。
- 6715 维断点不能由 v2.x 环境加载；跨版本对战用 `evaluate_cross.py`。
- 完整 checkpoint（32 万步起每 16 万步一个 + `best/` 为 10 万步评估 best）在 `rl/checkpoints/random/20260905-011908/`；TB 日志 `rl/tb/ppo_random_20260905-011908_resume_0`。

## 模型检查点与测试快照

这些文件是训练时间切片或接口测试文件，不是独立策略；观察、动作和兼容性与其所属模型一致。

- `rl/checkpoints/default/20260824-034840/`：v2.0.0，20,000–500,000 步，`best/` 为 500,000 步版本。
- `rl/checkpoints/default/20260825-053124/`：v2.1.1，20,000–500,000 步，`best/` 对应 480,000 步。
- `rl/checkpoints/default/20260826-050439/`：v2.1.3（defensive 对手），至 800,000 步。
- `rl/checkpoints/default/20260826-151230/`：v2.1.4，至 800,000 步。
- `rl/checkpoints/default/20260827-010723/`：v2.1.5，至 600,000 步。
- `rl/checkpoints/default/20260827-044352/`：v2.1.6，至 800,000 步。
- `rl/checkpoints/default/20260827-124430/`：v2.1.7→v2.1.8 续训运行（首次运行在约 13 万步处崩溃），至 920,000 步。
- `rl/checkpoints/default/20260827-182746/`：**v2.2.0，至 800,000 步；`best/best_model.zip` 即当前推荐模型的来源**。
- `rl/checkpoints/default/`：v1 随机对手的 20,000–120,000 步 checkpoint 和 best 文件。
- `rl/checkpoints/random/20260830-015922/`：v2.4.0（256 网络），至 2,000,000 步；`best/best_model.zip` 即当前推荐模型的来源（196 万步处的评估 best）。
- `rl/checkpoints/random/20260901-045740/`：v2.7.0（6205 维），至 4,000,000 步；`best/best_model.zip` 为 395 万帧交付模型。
- `rl/checkpoints/random/20260830-123944/`：v2.5.0（6024 维观测），至 2,000,000 步；评估 best（6 万步）互打平庸未采用，**交付的是 200 万步终点断点**（手工清洗）。
- `rl/checkpoints/random/20260831-034422/`：v2.6.0（对手生态加压），至 4,000,000 步；`best/` 为 356 万步评估 best（已交付）。
- `rl/test-output/`：历史评估脚本（`eval_candidates.py` / `eval_final.py` / `eval_random_rule.py` / `read_tb.py`），接口测试模型快照已清理。

检查点文件名中的 `_steps` 是保存时累计步数；正式模型文件名最后的数字是训练目标步数（续训产物的实际步数可能更高，以 zip 内 `num_timesteps` 为准）。自 v2.2.1 起，`rl/models/` 的交付文件为评估 best 断点而非训练终点。

## 所有模型共享的版本演进与路由规则

| 版本 | 关键变化 | 兼容方式 |
|---|---|---|
| v1.0.0 | Gymnasium + MaskablePPO；随机合法动作；`Discrete(512)` | 只能使用 v1 快照环境 |
| v2.0.0 | 规则对手；固定意图槽；奖励塑形；`Discrete(38)` | `env_v200.py` + `run_model_v200.py` |
| v2.0.1–v2.0.4 | CUDA 选择、断点/检查点、时间戳命名和 zip 后缀修复 | 不改变 v2.0 契约 |
| v2.1.0 | 即时奖励归因、BFS 移动、12 稳定槽、mixed 对手、`Discrete(54)`、相对视角 | 必须从零训练（该版本埋下移动一格与奖励吞占点两个缺陷） |
| v2.1.1 | 修复空槽治疗崩溃 | v2.1.0 模型可继续使用 |
| v2.1.2 | 保存 v1/v2.0 快照/运行器，按动作空间自动路由 | 512/38/54 分别使用各自运行器 |
| v2.1.3 | 统一模型文件命名，版本号和日期前置 | 新模型使用版本前置格式 |
| v2.1.4 | 引入 v2.0.0 模型对手（映射错误，已废弃实现） | 模型作废 |
| v2.1.5 | 模型对手改用 `legacy` 动作表（与 env_v200 逐格对拍验证） | 模型作废（环境另有缺陷） |
| v2.1.6 | 修复移动一步直达最优可达格 | 模型作废（奖励缺陷仍在） |
| v2.1.7 | 奖励恢复对手响应后结算，找回占点信号 | 该次训练因下条缺陷中断 |
| v2.1.8 | 对手动作被拒且游戏已结束时不再崩溃 | 模型作废（交替损坏环境） |
| v2.1.9 | 恢复动作后状态刷新，修复回合交替损坏 | 训练环境修复 |
| v2.2.0 | 座位随机化：智能体坐 player_b 时模型对手满血 | 产出当前推荐模型 |
| v2.2.1 | 训练收尾交付评估 best 断点而非终点模型 | 仅交付逻辑变化 |
| v2.3.0 | 接入对称随机地图；观测 3922→5974 维（331 格标准定序）；归一化随局配置动态化；单位格补攻击力特征 | 旧模型全部不兼容；v2.1.x/v2.2.x 改用 `env_v22.py` + `run_model_v22.py`，54 动作按文件名版本分流 |
| v2.6.0 | 观测回退 5974 维（v2.5 动作历史被证伪）；对手生态加压：自对弈默认 85%、快照近期加权、锚点按 `RL_ANCHOR_PROB`（15%）固定出场 | 6024 维断点改用 `env_v25.py` + `run_model_v25.py`；产出模型未超越 v2.4.0 且静态图遗忘，不作推荐 |
| v2.5.0 | 观测追加对手上一回合动作历史（5974→6024，源自事件日志）；worker 快照放行事件尾部；快照目录按版本隔离；候选对手按观测维度自适应编码 | 5974 维断点不兼容；v2.3/v2.4 改用 `env_v24.py` + `run_model_v24.py`；观测未带来提升（真换边 24 局 9:15 败于 v2.4.0），不作推荐 |
| v2.4.0 | 网络容量升级：`RL_NET_WIDTH`（默认 256，策略/价值网络宽度）；大缓冲配套（n_steps=512、batch=256） | 与 64 网络断点不兼容，从零训练；产出当前推荐模型 |
| v2.3.3 | 学习率线性衰减（`RL_LR_END`）；锚点对手 `RL_ANCHOR_MODEL`；快照按保存时间排序；交付清洗学习率闭包 | 不改变环境语义，v2.3.x 断点可续训 |
| v2.3.2 | 自对弈快照阶梯对手（`RL_SELF_PLAY_PROB`，默认随机图 60%）；`SnapshotCallback` 原子快照 | 不改变环境语义，v2.3.x 断点可续训；产出首个任意地图可战模型 |
| v2.3.1 | 并行训练：`RL_NUM_ENVS` 用 `SubprocVecEnv`，每环境一个引擎 worker；对手模型改传路径子进程懒加载 | 不改变环境语义，v2.3.0 断点可续训；产出随机地图模型 |

服务端 `src/api/bots.ts` 将 512 路由到 `run_model_v100.py`，38 路由到 `run_model_v200.py`；
54 动作按文件名版本三代分流：≥ v2.5 → `run_model.py`（6024 维），v2.3/v2.4 → `run_model_v24.py`（5974 维），其余 → `run_model_v22.py`（3922 维）；未知动作空间会被拒绝。
即使两个模型动作数相同，只要动作语义改变，也必须创建新的环境快照和运行器。

## 新增模型时的固定模板

以后新增模型请复制下面的结构，使用新的二级标题，不要只更新共享表格：

```markdown
## 模型 N：<模型文件名去掉 .zip>

### 身份与文件
- 文件、定位、地图、训练版本、训练日期、训练步数、文件大小、参数量、运行器

### 网络、观察和动作
- policy/value 网络、观察维度与编码、动作空间和 mask 语义

### 训练过程与超参数
- 环境、并行度、PPO 参数、设备、checkpoint、评估设置

### 训练对手与策略
- 对手脚本/风格、奖励、模型可能学到的战术和已知偏置

### 性能记录
- TensorBoard 指标、独立评估协议、胜率/回报/据点/动作统计和样本限制

### 部署与兼容性
- 运行器、允许座位、地图限制、能否续训、迁移方式
```

## 共同限制与后续方向

- 自 v2.3.1 起支持 `RL_NUM_ENVS` 并行环境（每环境一个引擎 worker，样本吞吐约 N 倍）；批量引擎（单进程多局）仍是进一步加速方向。
- **静态地图是验证集，不进训练分布**（明确决策）：自对弈提升绝对棋力而非混入静态图。**但 default 验证集本身的先手效应使 8 局样本失效**：历代随机图模型（v2.3.3 起）坐 player_a 对 v2.2.0 全部 0:8，后手才能赢；历史记录的 7:1/6:2 为小样本噪声。default 上的真实差距需用分座位大样本重估；先手崩溃的成因（随机图训练分布缺少固定布局先手经验）是后续方向。
- **评估样本量教训（v2.5.0 验收）**：同一配置重复 8 局互打结果可在 6:2↔4:4（随机图）、6:1↔1:7（default 后手）间摆动；单次 8 局无统计意义。“历代 5:3”“6:2 拉开”“8:0 碾压”“1:6 退化”均已被复测推翻或归入噪声。验收必须用累计大样本（≥24 局）或分座位统计，否则结论随机翻转。
- **evaluate_cross 座位 bug 教训（已修复）**：`--swap-sides` 第 2 局换边后重建控制器时固定取初始名字，第 3 局起座位不再往返，**历史上所有换边多局成绩全部作废**（包括历代记录的 7:1/6:2/5:3）。修复后真换边复测：v2.4.0 对 v2.5.0 随机图 15:9；v2.5.0 对 v2.2.0 default 先手 0:12/后手 9:3。任何旧换边数据需要重测才能引用。
- **训练期评估 best 不等于真实水平**：v2.5.0 评估 best（6 万步，峰值 80%）互打全 4:4、对规则 10:10；交付必须以互打为准。评估对手（规则）与自对弈对手池的非平稳强度差会放大早期评估虚高。
- **对手生态加压的天花板已确认（v2.6.0 实验）**：自对弈 85% + 快照近期加权 + 强锚点仍未能超越 v2.4.0（10:14），且高自对弈比例引发静态图灾难性遗忘（default 0:24，连后手局也丢）。观测扩展（v2.5）与生态加压（v2.6）两条路均已验证无效；随机图 + 自对弈阶梯配方的收益接近耗尽。后续方向需要更根本的改变（如批量引擎提速后的超长训练、更强的搜索/规划式信号、或重审静态图混入决策），继续在当前配方上加码没有意义。
- **高自对弈比例的副作用**：自对弈 ≥85% 时训练期评估（规则对手）峰值虚高更严重（85% 峰值但互打落后），且静态图能力快速遗忘；自对弈比例与静态图保持需权衡（60% 是历代验证过的平衡点）。
- 观察向量未显式编码完整攻击力、防御、移动范围和据点类型，策略只能从格占用、HP 和全局量间接推断。
- `special` 槽目前主要对应重装爆破；增加特殊技能时必须明确新语义并升版本。
- 训练后期存在胜率震荡（v2.2.0 峰值 90% → 终点 45%），已通过交付 best 断点兜底；后续可探索学习率衰减或早停。
- v2.0.0 只能在 player_a 满血，验收时固定该座位；评估应使用镜像局排除座位/先手因素（`default` 地图实测无先手优势）。
- v2.0 的旧视角限制无法通过后处理修复；可比结果必须使用各自快照运行器并保留兼容性警告。
